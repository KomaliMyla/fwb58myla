extends layout

block content
  h1= title
  p Welcome to the Lab on Pug
  button(type="button",onclick='update()', class="btn btn-warning")= 'Press Me'

  div(id="target") Display

  script.
    var client_count = 0;
    function update(){
      client_count ++;
      document.getElementById("target").innerHTML = "Value now " + client_count;
    }

 